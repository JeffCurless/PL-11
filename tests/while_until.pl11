BEGIN
    INTEGER I;
    INTEGER SUM;

    I := 1;
    SUM := 0;

    (* Sum 1..10, but stop early if SUM exceeds 20 *)
    WHILE I <= 10 DO
        BEGIN
            SUM := SUM + I;
            I := I + 1
        END
    UNTIL SUM > 20;

    PRINT('%d\n', SUM)
END
