BEGIN
    INTEGER I;
    INTEGER SUM;

    1 => I;
    0 => SUM;

    % Sum 1..10, but stop early if SUM exceeds 20
    WHILE I <= 10 DO
        BEGIN
            SUM + I => SUM;
            I + 1   => I
        END
    UNTIL SUM > 20;

    PRINT('%d\n', SUM)
END
