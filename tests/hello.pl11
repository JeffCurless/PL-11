% hello.pl11 — Minimal PL-11 program
% Computes a simple arithmetic result.
% PL-11 has no built-in I/O; output would be via
% device registers on the PDP-11.

BEGIN
    WORD X, Y, SUM;

    10     => X;
    32     => Y;
    X + Y  => SUM

    % SUM = 42
END
